import React from 'react';
import { View, Text } from 'react-native';

export default function SlotScreen() {
  return (
    <View style={{ flex: 1, padding: 20 }}>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}>Procurement Slot Booking</Text>
      {/* TODO: Add mandi selection and slot booking interface */}
    </View>
  );
}
